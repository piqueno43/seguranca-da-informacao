<!doctype html>
<html lang="pt-br">
   <head>
      <meta charset="utf-8">
      <meta name="viewport" content="width=device-width, initial-scale=1">
      <title><%=config.title%></title>
      <link rel="shortcut icon" href="/assets/images/shared/favicon.ico">
      <link rel="apple-touch-icon" href="/assets/images/shared/touch_icon.png">
      <% config.fonts.forEach(function(font){ %>
        <link href="/assets/css/<%=font;%>" rel="stylesheet">
      <% }); %>
      <% config.styles.forEach(function(style){ %>
        <link href="/assets/css/<%=style;%>" rel="stylesheet">
      <% }); %>
   </head>
  <body>
